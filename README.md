# Melonura

Reproduz `hehe.mp3` usando `ffplay` instalado no sistema.

Execução:

```bash
python3 a.py
```

Se o `ffplay` não estiver disponível, instale o pacote do FFmpeg da sua distribuição antes de rodar o script.